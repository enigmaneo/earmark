meta {
  name: Create Alignment Job
  type: http
  seq: 1
}

post {
  url: {{base_url}}/alignment/jobs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "abs_item_id": "{{abs_item_id}}"
  }
}
