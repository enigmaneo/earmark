meta {
  name: List Progress
  type: http
  seq: 2
}

get {
  url: {{base_url}}/web/progress
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

params:query {
  ~document:
  ~sort_by: updated_at
  ~sort_dir: desc
  ~page: 1
  ~per_page: 50
}
